# andyutaro.com — サイトの現状（2026-09-22 時点）

> 前提知識ゼロで読める自己完結メモ。別のClaude（claude.ai のチャット等）に状況を渡すためのもの。
> 作業用の細かい規約は `AGENTS.md` / `CLAUDE.md` に別途ある。

---

## 1. これは何か

**Andy（本名：安田裕太郎 / Yutaro Yasuda）のポッドキャスター/クリエイターとしての個人サイト**。本番 **https://andyutaro.com**。

目的は「時々気になって訪ねたくなる場所」であること。再訪の理由は2つ:

- **ポッドキャストの連続再生**（右側の PODCAST ピルから、選ばずに聴き始められる）
- **scribe** — 「読むポッドキャスト」。日々の考え事を**生放送で一文字ずつ配信しながら書き**、一日の終わり（0:01）に確定テキストになる。書いている最中は `/live` と Home の窓に、IME変換の途中経過ごとリアルタイムで流れる。

サイト全体が「**動く図（いま生きている＝ライブ）と静かな地（アーカイブ）**」の二層構造で設計されている。

---

## 2. デザインの原則（提案時に最も重要）

- 杉本博司的な**静けさ・細字（200/300のみ）・広い余白・グレースケール**。極端にミニマル。
- **彩色は原則LIVE赤のみ**。例外は2つだけで、どちらもAndy承認済み:
  - 番組ページのヒーロー映像（4番組の背景動画）
  - 注釈の青（`--anno`）
- **装飾・ラベルの追加は歓迎されない。** 見た目を1pxも変えない改善は歓迎。
- 新機能を足すときは**既存の視覚言語を使い回す**のが正解（新しい見た目を増やさない）。実例: 「同じ頃の作品」はPhotography一覧と同じ正方形グリッドを流用、番組の「舞台」は既存の「担当」ラベル行と同じ組み。
- ダークモード対応（`html[data-theme='dark']`、キーは `andy-theme`）。既定は時刻で決まる（6:01〜17:00ライト／17:01〜6:00ダーク、閲覧者の端末時計。`lib/site/theme.ts`）。手動切替はその選択が優先。
- 背景に**音の波形**が全ページ横断で走っている（fixed、z-index -1）。形はPODCASTピルの位置で決まる：**デスクトップ＝viewport縦中央を横切る一直線**でピルに接続。**スマホ＝ピルの左端から出る大きな円弧**（円の中心は画面右寄り・Y中央、左端に接する半径。右半分は画面外に見切れる。振幅はピルから離れるほど立ち上がる＝ボタンから音が出ている、2026-07-31 Andy案）。
- **固定UI（position:fixed）はデスクトップの外余白に住んでいる**（本文は最大720pxの中央柱）。**スマホには外余白が無い**ので、固定UIはそのまま本文の上に乗る。実測で常設4個（テーマ／MAIL／MENU／PODCAST）が本文柱346pxのうち64px＝18%を常時覆っていた（scribe窓の「全文を読む→」は完全に隠れていた）。→ **スマホに固定UIを新設しない。** 2026-07-31に**PODCASTピルだけ画面縦中央→右下**へ退けて衝突を解いた（右レール3つ＝テーマ／MAIL／MENUは右上のまま。一度MENUへ畳んだがAndyの指示で差し戻し）。641px以上は1pxも変えていない。
- **固定要素は「上から」の静的な指定で置く（2026-08-04）。** Chromeはスクロール開始でURLバーを畳む時、`bottom` 基準の固定要素だけを一緒にずらす。背景の波形は `top:0` 基準で動かないため、PODCASTピルを `bottom` で置いていた間は**スクロール中だけ両者が離れ、指を離すとパッと合う**という挙動になっていた。`svh`/`lvh` はURLバーの開閉で値が変わらないので、両方をこの単位で置けば動かない者同士になる。
- 右レールの3つは**塗り分けで役割が読める**ようにする: スイッチ＝沈んだ溝＋線、CONTACT/OTAYORI＝塗り（送る口）、MENU＝紙色＋濃い線（移動の口）。MENUを塗ると「全部くすんだ色」になってAndyに却下された（2026-08-04）——**視認性が足りない時は塗るのではなく線を濃くする**。
- MAILピルは中の語が **CONTACT ⇄ OTAYORI** に横スライドで入れ替わる（3.6秒。ワードマークの肩書き3.0秒と割り切れない値にして拍を重ねない）。ピル1つに2つの運動を持たせないため、以前の明滅は廃止。

---

## 3. 技術スタック

- **Next.js 16.2.10（改変版）** + React 19 + TypeScript
  - ⚠️ 通常のNextと異なるbreaking changeがある。**書く前に `node_modules/next/dist/docs/` の該当ガイドを読む規約**。`middleware.ts` ではなく `proxy.ts` 系（ただし現在proxyは廃止済み）。
- **ホスティング: Cloudflare Workers**（OpenNextアダプタ `@opennextjs/cloudflare` 1.20.1）。**Paidプラン $5/月**（2026-09-16に無料から移行。無料の CPU 10ms/リクエストの天井が Error 1102 の原因だったため。§4）。
- **DB/認証: Supabase**（`@supabase/supabase-js` 2.110）。無料プラン。**文章とデータだけ**（実測 約23MB / 500MB枠 ＝ 4.7%）。
- **メディア（写真・動画・音源）: Cloudflare R2** `rede-web-media` → **`media.andyutaro.com`** から配信（2026-08-29移行）。
  - 移行理由: Supabase Storageの1GB枠が**動画17本で83%**埋まり、月150〜280MBのペースで2か月後に満杯の見込みだった。R2は10GBで、しかも**下り転送が無料**＝観られ・聴かれるほど枠が減る構造にならない。Supabase Pro課金（$25/月）はAndyが却下。
  - Supabaseの原本（439MB）は**消していない**。旧URLも生きている＝R2側に何かあっても戻せる。
- **ライブ配信の中継: 自前WebSocketサーバー**（`rede-relay.onrender.com`、Render無料枠）。scribeの毎打鍵スナップショットを中継する。**唯一Cloudflare/Supabase外の稼働部品＝ここが落ちるとライブ中継だけ止まる**（書く・保存・公開ページは無事）。
- **メール通知: Resend**（おたよりの受信通知）。ドメインからの送信は封鎖してある（`MX .` ＋ SPF `-all` ＋ DMARC `p=reject`）。
- **費用はドメイン代（年額）＋ Workers Paid $5/月**（約750円。月1150円予算の内側）。旧STUDIOプランは解約済み、Vercelは完全撤収済み。Supabase・R2・Render・Resendは無料枠のまま。

### 実測の容量と使用量（2026-09-22）

| | 実測 | 枠 |
|---|---|---|
| Workers リクエスト | 月枠の見込み **2.1%** | $5に月1,000万込み |
| Workers **CPU時間** | 月枠の見込み **50.7%** | $5に月3,000万CPU-ms込み |
| Workers CPU（1リクエスト） | 中央値 10.1ms・平均 73ms・上位1% 585ms | 上限はPaidで30秒（最大5分） |
| R2 合計 | 1.45 GB | 10 GB |
| └ `rede-web-media`（メディア） | 0.63 GB / 435件 | |
| └ `rede-web-cache`（ISR・7日で失効） | 0.02 GB / 67件 | |
| └ `rede-web-backup`（控え・失効なし） | 0.80 GB / 520件 | |
| Supabase Storage（原本・増加停止） | 439 MB | 1 GB |
| Supabase Postgres | 約 23 MB（`studio_usage()` の db_bytes、索引込み） | 500 MB |
| Image Transformations | 未計測 | 5,000ユニーク/月 |

- **CPUの中央値10.1msは、無料プランなら壁ちょうど**。Paidに移る前は「普通の仕事の半分が天井付近」という状態だった（§4）。
- **$5の内側で効いているのはリクエスト数ではなくCPU時間**（2026-09-22に判明）。リクエストは月枠の2.1%なのにCPUは50.7%を使っている＝1リクエストあたり平均73ms。**リクエスト数だけ見ると安心して間違える。** 超えても破滅はしない（超過は$0.02/100万CPU-ms＝枠の倍使っても月$0.60程度）が、**次に重い機能を足すときの見るべき数字はこちら**。
- **`rede-web-cache` が0.24GB→0.02GBに縮んでいる**のは、7日失効と、作り直しの回数が激減したこと（Homeを1時間・他を1日に延ばした）の両方が効いた結果。

### Markdownは全面廃止（重要な確定事項）

本文のSSOT（唯一の真実）は**contenteditableが吐く生HTML**（`<br>`方式、全ブロックに `data-block-id`）。
理由: Markdownの「改行で空白を作らない」哲学が、Andyの「空白行の数で余白を作る」書き方と非和解的に衝突したため。
**Markdown変換・記法の導入はいかなる形でも提案しない。** 新機能で本文に記法を足したくなったら、別レイヤーで持つ（注釈がその実例）。

---

## 4. デプロイと運用

- **`git push`（mainブランチ）だけで自動デプロイ**（Cloudflare Workers Builds）。約2〜3分で本番反映。
- ローカルの `npm run deploy:cf` は**自動化が壊れた時の緊急用のみ**（併用すると版ずれ事故の元）。
- ⚠️ **短時間に連続pushすると最後のビルドがスキップされることがある**（失敗記録も残らない）。連続push後はダッシュボードのデプロイタブで最新バージョンが最新コミットと一致するか確認する。
- **Cron（2本、2026-09-14から）**:
  - ①**毎日0:01 JST**（15:01 UTC）`/api/cron/finalize`。中身は「scribe確定 → R2バックアップ → 孤児メディア掃除 → 再生キューの作り置き → ブックマーク同期＋タイトル取得」。
  - ②**3時間ごと**（`31 */3 * * *`）`/api/cron/backup-photos`。写真の控えだけを進める専用cron。`worker.js` の `scheduled` が起動時刻で行き先を振り分ける。
  - 分けた理由: 1起動あたりのサブリクエスト上限を確定・掃除・キュー・ブックマークで分け合うため、夜のcronでは写真が**1晩6枚**しか進まなかった（2026-09-14時点で未処理145枚＝追いつくのに3週間以上）。専用cronは上限を丸ごと使えるので1回20枚・1日160枚。追いついた後は毎回0枚で終わる。
  - ログの印は夜が `[cron]`、専用が `[cron-photos]`。**日次点検が `[cron]` の記録を読むので、夜のcronの形は変えない。**
- **Web Analytics有効**（cookieなし・bot除外）。**数字はAndyに見せない運用**（後述）。
- **日次点検（2026-08-29から常駐）**: `scripts/health-check.mjs` が毎朝9:03に走り、**異常があるときだけ通知**する。作った理由は「見られる状態にする」と「見る」が別だったから——observabilityは前から有効で、7日8,190件の失敗が数週間ぶん誰にも見られていなかった。
  - **分析API（GraphQL）の数字だけを見ない。** 裏側（`waitUntil`）の失敗はリクエストとしては成功に数えられるので、Observabilityのログを必ず併せて読む（実態の1/293しか映らない。§11-11）。
  - 判定は前回値との差分ではなく**しきい値**（前回値の保存が要らない＝仕組みが壊れない）。**アクセス数そのものは出さず、枠に対する割合だけ**（§9）。
  - **枠は月次で見る（2026-09-22改訂）**: リクエストは月1,000万、CPUは月3,000万msの込みぶんに対する**直近24時間の30日換算**。月初からの累計にしないのは、月のどこで走っても同じ意味の数字が出るため。**しきい値は100%**——超過は$0.02/100万CPU-ms程度なので、それ未満で鳴らすと通知が意味を失う。
  - 失敗は**人・bot・去った側**に分ける（2026-09-03）。分ける前はGPTBotの巡回101件がそのまま「読者に見えた失敗101件」として鳴り、こちらの判断も誤らせた。最優先の通知は「**人の前でWorkerが死んだ**」（exceededCpu/WallTime/Memory/internalError）だけ。
- **R2の自動掃除（2026-07-29設定 → 2026-08-05に7日へ短縮）**: `rede-web-cache`（ISRキャッシュ）のライフサイクル規則を `expire-isr-cache-7d`（7日で失効）に置き換えた。中身は全て再生成可能なので消えても無害。**`rede-web-backup`には規則を入れない**（バックアップが消えるため）。
  - 30日では足りなかった理由: 実測で**1日あたり約400MB・約630オブジェクト**の線形増加が続いていた（7/30 976MB → 8/1 1,794MB）。R2無料枠は10GBなので8月下旬に越える計算で、しかも30日規則が削り始めるのは8月末＝間に合わなかった。
  - 増加の正体は2つとも「鍵が増え続ける」形: ①`?rev=` の30分窓（下記）が30分ごとに新しいfetchキャッシュ鍵を作る ②OpenNextの鍵が `incremental-cache/<buildId>/<hash>.<種別>` なので**デプロイのたびに前ビルドのキャッシュが丸ごと孤児になる**。どちらも「二度と読まれない物が残り続ける」なので、失効期間を縮めるのが両方に効く唯一の低リスクな手。
  - **7日にした理由（2〜3日にしない）**: 失効はISRページの鍵にも等しくかかる。短くしすぎると、めったに見られない番組回ページ（300枚超）がキャッシュを失って**再生成＝1102のリスクが上がる**＝直したい当のものが悪化する。7日なら週1回でも見られたページは温かいまま保たれ、蓄積は約2.8GB（10GB枠の3割弱）で頭打ちになる。**この「1102のリスク」は2026-09-16のPaid移行で消えた**が、7日のままで困っていないので触っていない（実測0.02GB／§3）。
  - **buildIdを固定して孤児化を消す案は却下**。鍵にbuildIdが入っているのは、旧ビルドのHTMLが新しいコードに対して配られるのを防ぐため。固定すると版ずれ事故になる。専用の掃除スクリプトも、現ビルドの判定を1つ間違えると生きているキャッシュを消すので置かない（失効に任せる）。
- **Search Console登録済み**（ドメインプロパティ）、sitemap.xml送信済み（471ページ）。

### Error 1102（Worker exceeded resource limits）— 2026-09-16に解消

**現状（2026-09-22実測）: 0件。** 9/18以降ひとつも出ていない。日別の推移がそのまま原因を指している:

| 日 | exceededResources |
|---|---|
| 09-13 | 93 |
| 09-14 | 126 |
| 09-15 | 42 |
| 09-16 | 66 ←この日にPaidへ |
| 09-17 | 1 |
| 09-18〜09-22 | **0** |

**原因は無料プランの CPU 10ms/リクエストの天井だった**（公式仕様で変更不可。Paidは30秒・最大5分）。このサイトの普通の仕事はそれを超える——記事の保存12〜37ms、`/notes` 10〜66ms、`/photography/[id]` 平均27ms。**普通に動かすだけで天井を超えていた**ので、1102はおよそ100リクエストに1回の割合で出ていた。2026-09-16にはAndy自身の編集セッションにも当たり、`/notes` `/studio/notes` の表示と記事の保存36回が落ちた。

**2026-09-16、Workers Paid（$5/月）へ。** 「利用者がAndy一人のうちは課金しない」という前提は、実在の読者が毎日503を踏んでいる時点で外れていた。**無料枠に戻すための回避策を再提案しない。**（Supabase Pro $25/月を却下した判断とは別件。あちらは容量の話で、R2移行で解けている。）

#### そこへ至るまでの手当て（どれも無駄ではなく、いまも効いている）

- **2026-08-29** レイアウトからRSS取得を外す（描画の主因を断つ）。
- **2026-08-31 ISRの作り直しキューを設定**。`open-next.config` にqueueの指定が無く、既定の `"dummy"`（`send()` がFatalErrorを投げるだけの空実装）が使われていた＝**稼働開始から一度も作り直しが成功していなかった**。1日1,087件の `Failed to revalidate stale page` の正体で、「保存したのに反映されない」も同じ原因。**Durable Object版を選んだ**のは、service binding越しの呼び出しが**呼び出し元とCPU予算を共有する**ため（memory-queueだと作り直しの再描画100〜900msが閲覧者の10ms枠に乗る）。
- **2026-09-01 Homeの描画からRSSの往復10本を外す**。夜のcronが番組の要約（カバー・最新日付・直近24回）を `site_content` へ作り置き、Homeはそれを1本読む。裏側では1日41,528件のエラーが出ていて、**読者に見えた失敗は0件**のまま、Supabaseへ1日46,000リクエストが飛んでいた。
- **2026-09-02 作り直しキューの自励振動を止める**（`MAX_RETRIES=1` / `MAX_REVALIDATION=1` / `TIMEOUT 20秒` / 前段を60秒畳む）。DOQueueHandlerのalarmが失敗経路を1回でまとめて作り直し、無料プランのサブリクエスト上限50本を割って全部落ちていた。**有料プランは1,000本なので起きない＝無料プラン固有の症状**。
- **2026-09-03 遮断器**。詰まったDOを待つ `queue.send()` を1秒で見切って握りつぶす。依頼は落としてよい（古ければ次に来た人がまた投げる）——詰まったDOが読者を巻き込まないことのほうが重い。
- **2026-09-03 Homeのrevalidate 60秒→1時間**、**2026-09-06 他のISRページ 1800秒→1日**。作り直しの失敗は**試みる回数に比例**して出るので、機会そのものを減らした（2026-09-06 Andy指定「**エラーが起きないこと ≫≫≫ 即時反映**」）。

#### いまも変えない作法

- **重い計算は必ずエッジキャッシュに逃がす**（`lib/site/edgeCache.ts` の `cachedJson`）。天井が外れてもこれは変えない。
- **force-dynamicのページ**（`/notes` `/updates` `/photography` `/physical` `/desk/[date]` `/live` `/search` と各個別ページ）は毎回生成される。**Paidになった今はこれが危険ではない**（CPU実測で `/live` 299ms・`/photography` 435ms、どちらも余裕）。ISR化は目的ではなくなった。
- **「RSSが2MBを超えてNextのデータキャッシュに入らない」警告は1102とは無関係**（2026-08-05に確認、混同しやすいので明記する）。`loadFeedXml` はフェッチ後に必ず `parseFeed(await res.text())` を通るので、**データキャッシュが効いてもパースは毎回走る**。CPUを食っているのはパースの方で、それは既にエッジCache API（パース済みJSON）が受け持っている。データキャッシュが救えるのはネットワーク往復だけ＝WorkersではI/O待ちでCPUに計上されない。**この警告を追ってCPUの話にしない。**
- 2026-08-29に確認した「**10msは生きている**」（Observabilityに `Worker exceeded CPU time limit.` がそのまま出ていた／分析APIの「失敗側のCPU中央値10.0ms」は低いのではなく**壁ちょうどで殺された**という意味）は正しい観測で、そのままPaidへの判断材料になった。

### ISRの代金＝「訪問者はひとつ前の訪問者の時点を見る」（2026-08-05、Andy判断済み）

ISR化したページは `s-maxage={revalidate}, stale-while-revalidate=2592000`（**SWRの幅が30日**）を返す。意味は「revalidateを過ぎたキャッシュに最初に当たった人には**古いほうを即座に返し、裏で作り直す**」。新しいものを受け取るのは次に来た人。したがって**低アクセスなこのサイトでは、訪問者は基本的にひとつ前の訪問者の時点を見ている**。

- 実際に踏んだ症状（2026-08-05）: 番組を更新した2時間後、公開サイトのHomeが更新前の日付を出していた。**本番を連続で叩くと最初の3回が `x-nextjs-cache: STALE` で古い値、4回目以降が `HIT` で新しい値**、という形で再現する。
- **これはfeedの遅れでもパースの不具合でもない。** その線は潰してある——Anchorは問い合わせ方に関わらず同じ実体を返し、その中に新しい回は入っていて、`parseFeed` もそれを正しく拾う。**同じ症状を見たら、まず `x-nextjs-cache` を見る**（feedを疑って時間を溶かさない）。
- 由来: これは今夜のISR化が持ち込んだもの。旧 `force-dynamic` は毎回作り直すので常に最新だった代わりに1102を出していた。**鮮度とCPUを交換した結果**であって、壊れているのではない。
- 直す手段は2つ知られていた。**片方（Workers Paid $5/月）は2026-09-16に実行済み**（§4）。もう片方の `next.config.ts` の `expireTime`（SWRの幅を縮める）は未着手で、入れるならページごとに幅が違うことの検証込み。
- → **この挙動を「バグ」として直しにいかない。** 据え置きは設計上の代金であって故障ではない。

**2026-09-22 現在のrevalidate**（2026-09-06にAndyが決めた優先順位「エラーが起きないこと ≫≫≫ 即時反映」の結果）:

| ページ | revalidate |
|---|---|
| Home | 1時間 |
| `/podcast` `/podcast/[slug]` | **30分**（2026-09-22に1日から戻した。新しい回が"現れる"ページ） |
| `/podcast/[slug]/[episode]` `/podcast/guest/[id]` `/about` `/mail` `/membership` `/privacy` | **1日**（回の個別ページは公開後に中身が変わらない＝鮮度を買っても何も増えず、330枚ぶんの作り直しが増えるだけ） |
| `/notes` `/updates` `/photography` `/physical` `/desk/[date]` `/live` `/search` と各個別ページ | force-dynamic（毎回生成） |

SWRは30日のまま。**据え置きは2026-08-05の当時より長い**（新エピソードが番組ページに出るのも最大1日）。**Paidになった今は短く戻せる余地がある**——天井が外れて作り直しの失敗が出なくなったため。ただし戻すなら1つずつ、実測を見ながら（§10）。

### 新エピソードの反映は「配信後最大1〜2時間」（2026-08-13改訂）

8/12 20:00配信のサカナカイギ新回が、11時間後も/updatesとHomeに出ない障害が起きた（番組ページはISR再生成で出ていた）。原因は二段:

- **AnchorのCDNは多層で、内側の共有キャッシュはクエリを無視する**（実測: 初見のrev付きURLに `age: 34875`・`x-cache: MISS, HIT, HIT`）。7/29対策の `?rev=` 30分窓が破れるのは外側だけ。配信直後、内側が古い実体（s-maxage 7日）を返す間（実測76分）は、どんなURLで取っても古いXMLしか出ない。**→「30分以内に反映」は外的要因で保証不能。正直な期待値は配信後最大1〜2時間**（司令塔に通す仕様注記）。
- その76分間に取り込んだ古いXMLを、**fetchの `next: { revalidate: 1800 }`（OpenNext+改変Next16のデータキャッシュ）が期限を過ぎても返し続けた**。他の層（インメモリ30分・拠点エッジJSON 30分）は期限どおり切れることをコードで確認済み＝消去法でこの層。
- 対応: フィード取得からデータキャッシュを外した（オプション無しfetch＝このNextでは「auto no cache」）。**`cache:'no-store'` は使えない**——no-storeを1つでも含むルートは動的化するとISRガイドに明記されており、/podcast配下のISRが毎リクエスト再生成に戻って1102が再来する。取得頻度は自前の2層が従来どおり抑える（ルート表はビルド前後で完全一致を確認、2MB超でデータキャッシュに書けない警告も消えた）。
- 余禄: R2の「?rev=窓が30分ごとに鍵を増やす」蓄積（§R2の自動掃除）は、データキャッシュに書かなくなったので今後は発生しない。

---

## 5. ページ構成

**公開**（`app/(site)/`）
`/`（Home） `/about` `/podcast` `/podcast/[slug]`（番組） `/podcast/[slug]/[episode]`（回） `/podcast/guest/[id]`（ゲスト出演の回）
`/notes`（記事＋scribe一覧） `/notes/[id]` `/photography` `/photography/[id]`
`/physical`（物理作品） `/physical/[id]` `/desk/[date]`（確定scribe） `/live`（当日の生放送）
`/updates` `/membership` `/contact`（CONTACT & OTAYORI） `/search` `/privacy`

`/live` と `/desk/[date]` は下部のPagerで相互に繋がっている（2026-08-04）。`/live` の「← 前」は前日の確定アーカイブ、**いちばん新しい確定アーカイブの「次」は `/live`**（表示は `Desk Live Today`、赤い点が明滅する＝確定した過去ではなくまだ書き換わりうる今日、の意）。

**管理**（要ログイン）
- `/desk` — **放送卓**。scribeを書く場所。Andyはこれをデスクトップ/モバイルアプリ化して使っている。右下のボタンは**メニュー**（画像・動画・PDFの追加／編集室へ）。**行き先を押すと、デバウンス待ちの保存を送り切ってから出る**——クライアント遷移では `beforeunload` が走らず直前の数秒ぶんが落ちるため（スマホでは `beforeunload` の発火自体が当てにならない）。
- `/studio/*` — **編集室**。8室: NOTES / PHOTOGRAPHY / PODCAST INBOX / UPDATES / **BOOKMARKS** / TAGS / MAIL / USAGE
  - **スマホでは一覧を「表」ではなく「カード」に組む（2026-08-04）。** studioの一覧はどれも固定幅の列を横に並べた表で、列の合計が画面幅を越えていた（MAILの行はチェック15+日付+状態44+種別62+名前110+メール190+本文で500px超）。ヘッダー行は隠し、固定幅は解く。**スマホ節は `studio.css` の末尾に置くこと**——中間に置くと後続の定義（CONTACT室など）に上書きされて丸ごと効かない。
  - MAIL室は既定が「全件」。**仕事の相談とおたよりは種別で分かれる**（おたよりフォームは `topics` を「おたより — 宛先」の形で送るので、データを足さずに判別できる）。

**認証**: Supabaseのワンタイムコード（メールリンクはGmailのスキャナがトークンを消費するため主経路にしない）。サーバーコンポーネントのガードが唯一の門。

---

## 6. コンテンツの単位

### 番組（`lib/site/shows.ts`、全てAnchor RSSから自動取得）

| 区分 | 番組 | 状態 |
|---|---|---|
| ORIGINAL（本人の番組） | サカナカイギ | 継続中 |
| | ロングポスト | 継続中 |
| | ミモリラジオ | 終了番組 |
| | ガイロン | 未配信（フィードURL待ち） |
| WORKS（制作参加） | ON-AIRDO 声で旅する北海道 | 継続中 |
| | Brand Shift | 継続中 |
| | オルタナティブフィッシング | 未配信 |

**GUEST（ゲスト出演）は `shows.ts` に入れない。** 他人の番組なので番組リストではなく `guest_episodes` テーブルが持つ（→ §7「ゲスト出演」）。番組タイルにも並べない。

番組ごとにページの性質が分岐していく方針。番組ページが持つもの:
- **ヒーロー映像**（4番組。背景に沈めた映像の上を波形が泳ぐ）
- **舞台**（場所＋座標の銘板。女川 / 白老 / 北海道 / NY⇄東京）
- **STARTERS（入門3選）**、**PRODUCTS（番組から生まれた物理作品）**、ROLE、SHOW NOTES
- 節順: **WORKS系** = SHOW NOTES→ROLE→STARTERS（制作参加は担当の提示が先）／**ORIGINAL系** = STARTERS→PRODUCTS

### 棚（articlesテーブルを `type` で区別）

- **Notes** = `article`（記事）＋ scribeアーカイブ
- **Photography** = `photography`（下位区分 `photo_kind`: artwork / photolog）
- **Physical** = `physical` ＋ `event`（下位区分タブ: OBJECT / EVENT）

**Events棚はPhysicalへ統合（2026-08-07 Andy承認）。** Physical 4件・Events 5件と薄い棚が2つ並んでいたのを、棚の定義を「物理作品のアーカイブ」から**「物理世界にあるもの・あったこと」**へ広げて1つにした。中は **もの（OBJECT）と こと（EVENT）** のタブ。ナビは9項目に（Eventsが消えた）。DBの`type`は`physical`/`event`のまま＝**棚名だけの変更**（Article棚→Notes改名と同じ作法）。旧URLは`next.config.ts`で308（`/events`→`/physical`、`/events/:id`→`/physical/:id`）。studioは`/studio/physical`と`/studio/events`の2部屋のまま（「部屋がtypeを決める」設計を崩さないため）。

- 検討して**採らなかった案**: Photographyも含めた3棚統合（Photographyは132件で、混ぜるとNotes/Physicalの性格が消える）／棚名を`Works`（番組ページのWORKS棚と衝突）／棚名を`Expansion`（中身ではなく由来を語る名前で、初見に中身が見えない。「番組から生まれたもの」という由来の提示は番組ページのEXPANSION節が既に担っている）。

---

## 7. 主要機能（現在すべて本番稼働中）

### Homeの並び（2026-09-11 Andy指定）
PODCAST — ORIGINAL / WORKS → **UPDATE — LATEST 5** → **PODCAST — LATEST** → **NOTE — LATEST** → DESK（当日の窓）→ PHOTO
- **PODCAST — LATEST**: `/podcast` のエピソードタイルの最新4件。自番組とゲスト出演を公開日で混ぜる。**HomeはRSSを引かない**（サブリクエスト上限で落ちた前科がある。§4）——自番組は夜の作り置き（`showSummary`）、ゲスト回は控え（`listGuestRows`）から読む。
- **NOTE — LATEST**: `/notes` のALLタブの最新4件（当日のLIVEセルが先頭）。**Homeは読むだけ**でサムネイルの焼き込みはしない。
- **スマホ（640px以下）は両方3件**。タイルは3列で、4件だと3枚＋取り残し1枚になる。列数は変えない（2列＝1枚が153〜181pxの「重いカバー」に戻るという2026-07-14の判断）。隠れた分は「ALL →」から。
- UPDATEは**新しい順に5件**（日数で絞らない＝更新の少ない週に「LATEST 5」なのに3件、を避ける）。**DESKは当日のLIVE行も確定アーカイブも載せない**（2026-09-22 Andy承認）——すぐ下のDESKの窓と NOTE — LATEST が担うので重複であり、それ以上に**毎日1本出るDESKが5枠を必ず埋め切って、その週に配信した番組の回が1本も載らなくなる**（実測: 9/17〜9/21の5行が全部DESK、同じ週の4本は全滅）。同じ日付で並んだときもDESKが先に積まれるので、構造として回は永久に出てこなかった。`/updates`（全量の記録）は変えない。
  - 経緯: 2026-07-20に「毎日のscribeで埋まりすぎる」として当日分だけにする → 2026-09-11に確定アーカイブを戻す → 同じ問題が再発してまた外す。**この欄にDESKを戻すと必ずこうなる。**
- 「続き」の行き先は**節の右下**（`.section-foot`）で統一し、字面もDESKの窓に合わせて「全文を読む →」に揃えた。
- 部品は棚と同じものを使う（`PodcastEpisodeGrid` / `ArticleGrid` に `heading` / `allHref` / `limit` を渡すと、タブ・検索・件数を出さず「見出し＋ALL →」だけになる）。**棚の側は何も変わらない。**

### scribeライブ配信
毎打鍵の**全量スナップショット**をWebSocketで中継（差分同期にしない＝壊れにくさの根拠）。IME特別扱いコードはゼロ（contenteditableが未確定文字列をDOMに描くので変換過程が自動で乗る）。一方向性はサーバー構造で担保（sub側から来たデータはping/close以外破棄）。

### 注釈（2026-07-28、稼働中・129件）
本文の一部をドラッグして後から欄外注釈を足す。
- **本文（SSOT）には一切触れない別レイヤー**。記法もタグも増えない。消せば跡形もなく消える。
- アンカーは「本文全体の先頭からの文字オフセット＋引用文字列」。本文を後から編集してもズレを引用文字列で再探索して吸収（W3C Web Annotationのtext quote selectorと同じ考え方）。
- 表示: 引用範囲に**青い下線**→タップで小さくポップアップ（右端寄せ＝文頭に被らない）。書き込んだ日時つき。
- **ログイン中は公開サイトから直接追加できる**（`andyutaro.com` を普通に見ている画面でドラッグ→「注釈」）。書き込み可否は必ずAPI側で再検証。匿名訪問者にはCookie有無だけ見て即false＝Supabaseに問い合わせない。
- 同じ箇所への二重引きは断る。段落をまたぐ選択は可。
- **当日のdesk（`/live`）にも引ける（2026-08-04）。** 宛先キーは確定アーカイブと同一（`kind:'scribe'` + 日付）なので、0:01に確定した後もそのままアーカイブ側に出る。実装上の勘所が2つ: ①ライブの差分エンジンはDOMと受信HTMLの `outerHTML` を比べるので、注釈spanが入ったままだとそのブロックが毎回「変わった」と判定され画像や動画ごと作り直される。パッチの直前に `clearAnnotations()` で外し、直後に貼り直す（外したあと `normalize()` でテキストノードを繋ぎ直すのが要点）②キャレット `▍` は注釈を貼る前に外す（本文に混ざると文字位置がずれる）。
- 注釈の**操作**（タップで開く／選んで足す／書く／消す）は `app/(site)/AnnotationLayer.tsx` に切り出して確定アーカイブとライブで共有。**下線を引く仕事は本文を描く側**（アーカイブは一度描いて終わり、ライブは打鍵ごとに貼り直す、と事情が違うため）。
- 選択の検出は `mouseup`/`touchend` に加えて `selectionchange` も見る（長押しで選ぶ端末は選択の確定が `touchend` の後で、掴みの移動はOS側UIなのでイベントが来ない）。**選択が消えてもボタンは引っ込めない**——アンカーは選んだ瞬間に確保済みで、ライブは打鍵のたびに選択が消えるため。

### 作業用まとめ聞き（2026-08-04、稼働中）
番組ページ「第1回から作業用まとめ聞き」／エピソードページ「この回から作業用まとめ聞き」。旧「この番組を連続再生」（シャッフル10本を10:00地点から）を作り直したもの。**作業中のBGMという聴き方ではランダムも途中再生もノイズ**（Andy）。
- 並びは**放送順（古い回→新しい回）**。「#1から」「前に聴いた回の続きから追いつく」がそのまま成立する向き。
- 本数の上限なし（番組まるごと。サカナカイギで68回）。1本目も頭から鳴らす（イベントの `fromStart`）。PODCASTピルの全番組キューが持つ「放送に途中から合流する」10:00シークはそちら側に残っている。
- 再生中の画面に**回の一覧**を出す（現在行は赤・中央に自動で寄る・1タップで移動）。長いキューでは点列と矢印は畳む——点と矢印は近くを1つずつ動かす道具で、68回からEP30を出そうとすると何度も押すことになるため。

### ゲスト出演（GUEST群、2026-09-10、稼働中・6件）
Andyが他番組に出た回を、**SpotifyのエピソードURLを貼るだけ**で載せられる。置き場所はPodcast棚の第3群 `GUEST`（ORIGINAL / WORKS に続く）。**ナビは増やさない**——番組カタログがポートフォリオを兼ねる確定仕様のため。
- **番組タイル（カバーのグリッド）にはしない。** ORIGINAL/WORKSのタイルは「Andyが作っている番組」の記号で、他人の番組が同じ顔で並ぶと種別が壊れる。エピソードのタイル（`PodcastEpisodeGrid` のGUESTタブ）は記号が別なので同じ文法に乗せられる。並びは自番組の回と同じ**公開日降順**（サイトに追加した日順ではない）。2026-09-16に頭4件の上限を外して**全件**並べる（年に数回なので数行で収まる）。
- **解決の道筋**: Spotify URL → og:title（回名）/ og:description（番組名）→ iTunes Search APIで番組名から feedUrl → RSSを回名で突き合わせ → enclosure・日付・尺・カバー。RSSが見つかれば**サイト内で全編鳴る**（`AudioPlayer`、自分のエピソードページと同じ器）。見つからなければ送客ボタンのみ。
- **Spotify独占配信（RSSが存在しない番組）**は埋め込みページの `__NEXT_DATA__` を一次情報にする（回名・番組名・公開日・尺・カバー。音源だけが無い）。
- **Spotify埋め込みは使わない**（非ログイン訪問者にプレビューしか鳴らさず、紫のカードとロゴが入る＝彩色はLIVE赤のみの原則と衝突）。送客は別ボタン。
- **DBが持つのは feed_url ＋ episode id ＋ 送客URL と、取得に失敗したとき用の控え**（題名・番組名・公開日・尺・カバー）だけ。**本体はRSSが真実**（`episode_tags` と同じ考え方）。テーブルは `guest_episodes`。
- 入力はstudioの PODCAST INBOX にGUESTタブ（**部屋は増やさない**）。UPDATESにも流れ、sitemapにも載る。
- **CSPの `media-src` はホスト列挙**なので、Anchor以外に音源を置く番組は**無音で失敗する**。貼った時点で気づけるよう、studioのプレビューにホストの警告を出す。

### 同じ頃の作品（2026-07-28、稼働中）
日付を鍵に全ページを相互リンクする。各個別ページの下部に、その日付の**前後7日**に生まれた他の仕事を最大6件、**Photography一覧と同じ正方形の画像グリッド**で出す。
- 厳密な同日にしない理由: scribeは毎日・Physicalは年数回と頻度が違いすぎ、同日一致だとPhysicalでは一生表示されない死んだ機能になるため。
- 同じ棚は出さない（前後移動はPagerの担当）。
- 日付索引をエッジキャッシュ（`dated-index-2`、30分）に持つ。

### PHOTOLOG（2026-07-25、稼働中）
「scribeに写真を上げていることこそフォトログ」（Andy）。`/photography` のPHOTOLOGタブは、公開中コンテンツの本文に載っている**全アップロード写真**（サムネイルの1枚に限らない）を新しい順に流す。タイトルなし・日付のみ。タップで掲載ページの当該写真へスクロール。
- **日付は「掲載ページの日付」、並びは「本文に載っている順」**（2026-09-09に修正）。以前はアップロード日（R2のパスのフォルダ名）とuuidの文字列順だったため、日をまたいで写真を足すと**同じページの写真が2つの日付に割れていた**（実測: 9/7のdeskの51枚のうち14枚が「09.08」として先頭ブロックに切り離されていた）。
- **並びは「撮った日順」のまま**。「最近サイトに加わったもの」を上に出す（入れた順）への変更は2026-07-29に検討してAndyが却下した。**再提案しない。**

### BOOKMARKS室（2026-07-27、稼働中・94件）
「自分の文章に貼ったリンク＝自分のブックマーク」（Andy）。scribe/記事の本文から外部リンクを走査して集約。リンク先タイトルは夜間cron＋手動ボタンで少しずつ取得（Workers無料のサブリクエスト制限のため一括しない）。検索・ドメイン絞り込み・非表示。**本文がSSOT**（本文からリンクが消えたら行も消える）。

### 迷惑メール・罵倒の自動隔離（2026-07-27、稼働中）
`lib/site/spamFilter.ts` のルールベース判定（**AI不使用**＝費用ゼロ・理由が説明できる）。
- **隔離分はメール通知を送らない**＝Andyの目に入らない。**消さずに隔離**（studioのMAILにSPAMタブ、「スパムではない」で復帰可）。隔離理由を表示＝判定を監査できる。
- **批評は通す**: 「つまらない」「期待外れ」等の否定的な感想や、激しい語を含む長文の訴えは弾かない。弾くのは短文＋強い攻撃語の中身のない罵倒だけ。
- 本番でスパム投稿を実際に投げて隔離を検証済み。

### 本文はサーバー描画にも出る（2026-07-29）
本文の表示はクライアントのサニタイザがDOMに注入しているため、**サーバーが返すHTMLには本文が1文字も入っていなかった**。ブラウザでは問題ないが、JSを実行しない読み手（AIクローラー・テキスト抽出・エージェント）には空のページに見え、実際に別のClaudeが「0字・AWAY」と誤読した。robots.txtでAIクローラーを歓迎している方針とも矛盾していた。
対策: `lib/site/serverBody.ts` が**テキストのみ・全エスケープ・属性ゼロ**の本文をサーバー描画に出す（XSSの余地が構造的に無い＝クライアント側のホワイトリスト判断をサーバーで再実装しない設計）。クライアントはマウント後にリッチ版（画像・リンク・埋め込み）へ差し替えるので、人が見る画面は変わらない。字数カウンタもサーバーで実数を出す。生存表示は`scribe_days.updated_at`が3分以内なら「書いている」と近似（接続後は中継の値が上書き）。対象は `/live`・Home窓・scribe個別・記事3棚。

### 検索結果（SEO）
全ページに description ＋ canonical。JSON-LD は Person / WebSite / PodcastSeries / PodcastEpisode / BreadcrumbList ＋ `/about` の ProfilePage ＋ `/podcast` の CollectionPage（番組一覧。**ゲスト出演は他人の番組なので入れない**）。
**文言は全てAndyの既存テキストからの組成**（AI散文をサイトに載せない原則）。

**2026-09-10に直した3件:**
- **Xのカードに番組・エピソードの画像が出ていなかった。** og:image が `/api/og-image` を指していて、robots.txtの `Disallow: /api/` に入っていた。Twitterbotはrobots.txtに従い、画像URLが塞がれているとサムネイルを出さない。対象は**番組5＋エピソード約330**＝sitemapが「一番拾われてほしい」としているページ群そのものだった。`Allow: /api/og-image` を `Disallow: /api/` より前に置いて解決（リダイレクト先の `/cdn-cgi/image/` は元から塞いでいない）。
- **存在しないURLが200を返していた（ソフト404）。** 原因は `(site)` 直下の `loading.tsx`——全ページがSuspenseの内側に入り、**フォールバックがストリーミングされた時点でステータス200が確定する**ため、`notFound()` はnoindexを差し込むことしかできない（Next.jsの仕様。実測で、外すと404・残すと200）。波形のローダーは捨てずに `SiteLoading.tsx` へ移し、**可変の子（`/[id]` 等）を持たない7フォルダ**（about/live/mail/membership/privacy/search/updates）にだけ置いた。**desk/notes/photography/physical/podcast と Home には置かない**——置くと子の詳細ページまで包んで200に戻る。
- **sitemapのlastmod**を「生成した瞬間」から「中身が最後に変わった日」へ（30分ごとに全固定ページが更新されたと申告していた）。Home/Updates＝サイト全体の最新、Podcast＝最新エピソード、他は書かない。ゲスト出演の回も載せた（控えだけ読む＝RSSを引かない）。

### その他
- **単独URLの再生カード化**: Spotify/Apple/YouTubeのURLが単独で貼られたブロックは、**表示側で**再生カードに昇格する（`lib/site/upgradeEmbeds.ts`）。スマホの貼り付けがpasteイベントを通らない場合の救済で、SSOTには触れないので過去のアーカイブも遡って直る。
- **おたより**: 仕事の相談と番組へのおたよりを `/contact` で受ける。宛先は「【番組名】＋項目」で統一。ON-AIRDOのみ外部Google Form。
- **Aboutの末尾に窓口（2026-08-05）**: Aboutは「入口にはならないが、来た人はよく読む」面なので、読み切った先に `/contact` への導線を置いた。それまでの終着はOVERVIEW内の mailto 一本だけで、隔離フィルタを通らない経路に人を出していた。見た目は増やさず、**`/contact` が冒頭で出すモードタイル（`.mail-modes`）をそのまま借りる**＝「仕事の相談」と「番組へのおたより」の両方が送れることが一目で分かる。右のタイルは `?mode=otayori` でおたより面に着地する。
- **番組タイルのON AIR表示（2026-08-05 Andy「三日以内に更新されている番組は、その日付にエフェクトと色と小さな動きを。humbleな範囲での遊び心」。窓は同日中にAndyの指示で4日へ広げた）**: Home と `/podcast` の番組カバーで、**4日以内に更新された番組だけ日付がLIVE赤になり、小さなランプが灯る**。新しい色も形も作っていない——色はサイト唯一の彩色であるLIVE赤（「いま生きている」の意味がそのまま合う）、形はエピソード一覧の `.new-dot`（7日以内の新着）の使い回し、動きは滲み（box-shadow）がゆっくり呼吸するだけ。**明滅の周期2.4sはUPDATESの当日ドットと同じ**にして、サイト内の「生きている」の合図が全部同じ速さで動くようにしてある。
  - 窓が番組=4日／エピソード新着=7日と違うのは意図的（番組タイルは「いま動いている番組」を短い窓で示す方が意味が立つ）。
  - 境界は `CoverGrid` の `onAirSince` propで受ける。**`tokyoDaysAgo(4)` の呼び出しはページ側に置く**＝`Date.now()` をrenderに持ち込まない（`lib/site/text.ts` の規約、`EpisodeIndex` 等と同じ形）。渡さなければ既定 `'9999-12-31'` で誰も光らない。

---

## 8. データとバックアップ

- **テーブル**（2026-09-22実測）: `scribe_days`(92日) / `articles`(41) / `annotations`(129) / `arrivals`(2,446) / `bookmarks`(94) / `scribe_day_versions`(15) / `episode_tags`(15) / `desk_private_notes`(7) / `guest_episodes`(6) / `contact_messages`(5) / `site_content`(5) / `manual_updates`(0)
- **メディア**: R2 `rede-web-media`。配信は `media.andyutaro.com`。控えは `rede-web-backup/media/` へ差分コピー（**2026-08-29に取り出し元をSupabase→R2へ切り替え。切り替え前は新規アップ分の控えが取られていなかった**）。**2026-09-14から専用cron（3時間ごと）が担当**——夜のcronでは1晩6枚しか進まず追いつかなかったため（§4）。
- **Supabaseの `scribe-media`（439MB）は移行前の原本として凍結**。もう増えず、掃除の対象からも外した＝R2に何かあったときの戻り先として残す。
- **メディアを扱うコードはURLの形で判定しない。** 必ず `lib/site/media.ts` の `mediaPathOf()` でパス（`YYYY-MM-DD/uuid.ext`）に落として突き合わせる。パスは新旧のURLで同一。→ §11-6
- **RLSは全テーブル/ストレージで有効**。公開キーで読めるのは公開済みコンテンツのみ、書き込みは全拒否。
- **画像は上限1600px・JPEG q0.82で保存**（2026-07-29に2000px/q0.85から強化。表示側の最大は`IMG_W.photo`=1280なので画質は落ちない）。Storage無料枠1GBの消費ペースを落とすため。**既存ファイルは変わらない＝新規アップロードから効く**。
- **バックアップ**: 文章は毎晩0:01のcronで R2（`rede-web-backup`）へ日付つきJSONの世代保存。メディアは3時間ごとの専用cronがR2から差分コピー（1回20枚・1日160枚）。差分で写すので夜のcronと二重にはならない。Supabase無料プランに時点復旧が無いための備え。
- **未参照メディアの掃除もR2が対象**（2026-08-29に切り替え）。Supabaseを見ていた頃のままだと新規分の孤児が永久に溜まり、掃除が実質止まる。

---

## 9. Andyの確定した好み・制約（提案前に必ず確認）

- **「ラジオ/RADIO」という語を使わない**（本人が忌避）。代わりに PODCAST / ON AIR / 連続再生 / 放送。
- **アクセス数字を本人に見せない**。月1でClaudeがWeb Analyticsを読み、数字抜きの助言だけ渡す（スケジュールタスク化済み）。
- **図鑑（タグ索引）・関連回は当面やらない。提案もしない**（判断済み）。
- **本番に破壊的なセキュリティ検証をしない**（読み取り専用で確認、書換・削除は事前許可）。
- **AIの役割は整理のみ**（分類・ラベル付け・関連付け）。意味づけ（解釈・散文の執筆）は常にAndy本人。**AI生成の散文をサイトに載せない**。エピソード要約のAI生成も不採用。
- ミニマルさが最優先。装飾やラベルを増やす提案は歓迎されない。
- **移行関連は全て完了済み**（STUDIO解約・Vercel撤収・ドメイン紐付け）。**これらを再提案しない。**
- **Workers Paid（$5/月）には2026-09-16に移行済み。無料枠に戻すための回避策を再提案しない**（§4）。「利用者がAndy一人のうちは課金しない」という以前の方針は、実在の読者が毎日503を踏んでいる時点で外れた。ただし**課金が既定の解になったわけではない**——Supabase Pro（$25/月）は却下されたまま、容量の話はR2移行で解いた。
- **PHOTOLOGの並びは「撮った日順」のまま**（2026-07-29に「入れた順」を提案して却下）。並び替え・「新着写真」的な導線を提案しない。

---

## 10. いま開いている論点

- **Paidで緩められる設定の再検討（2026-09-22）**: 無料プラン固有の制約に合わせて絞った設定が残っている。**どれも今は困っていないので、やるなら1つずつ実測を見ながら**（まとめて戻すと何が効いたか分からなくなる）。
  - ISRのrevalidate（Home 1時間 / 他1日）を短く戻す＝反映を早くする
  - 作り直しキューの `MAX_RETRIES=1` / `MAX_REVALIDATION=1` / `TIMEOUT 20秒`（サブリクエスト上限が50→1,000になり前提が変わった）
  - ブックマークのタイトル取得と写真の控えの1回あたりの件数（同上）
- **`media.andyutaro.com` はbot対策で非ブラウザのUAに403を返す**（2026-08-29確認）。今は無害だが、ここからポッドキャスト音源を配るなら緩める必要がある（再生アプリのUAが弾かれる）。
- **未配信2番組**（ガイロン / オルタナティブフィッシング）: 配信開始後にフィードURLをもらって `shows.ts` に1行足すだけ。
- **公開Tagsページ: 保留**（2026-07-29 Andy「一旦いらないくらいで保留」）。**こちらから提案しない。**

※ここから外した論点（解決済み）: **Error 1102**＝2026-09-16のPaid移行で0件（§4）。**日次点検の分母が無料枠のままだった件**＝2026-09-22に月次へ改訂（そのときCPUが月枠の約半分を使っていると分かった。§3）。**残りの重いページのISR化**＝force-dynamicのままで危険ではなくなったので、目的そのものが消えた。**サカナカイギの所属行の視認性**（実機で問題なく読める）、**XのOGPキャッシュ**（新画像に更新済み）、**og:imageがrobots.txtで塞がれていた件**（2026-09-10修正、§7）。

---

## 11. 開発上の落とし穴（実際に踏んだ事故と教訓）

1. **`'use client'` でもSSRは走る。** `window`/`document` をコンポーネント本体のトップレベルで読むと、サーバー描画が毎回落ちてReactがクライアント描画にフォールバックする（**画面には出るので気づきにくい**）。触っていいのは useEffect・イベントハンドラ・条件付きJSXの中だけ。
2. **ビルド時に読まれるenvは `next.config.ts` のもの**（`NEXT_PUBLIC_SUPABASE_URL` / `SCRIBE_RELAY_URL` / `NODE_ENV`）。CI移行時にこれが欠けてCSPから中継が消え、ライブ配信が全停止した。現在は公開オリジンを既定値に持たせて恒久対策済み。
3. **重い計算はエッジキャッシュへ逃がす**（`cachedJson`）。force-dynamicページでコールドisolateだとメモリキャッシュは効かない。これが1102の主因だった（1102自体はPaid移行で消えたが、**作法は変えない**）。
4. **検証はブラウザペインを過信しない。** Claude Codeのペインはdevページをハイドレートせず、JS計測が0や異常値を返したりスクリーンショットが白紙になることがある。確実な検証は headless Chrome（`--dump-dom` / `--screenshot`）を使う。
5. **CSSチャンクは複数に分かれる。** 本番で特定のCSSが載ったか調べるときは、ページが参照する全 `.css` を走査する（1つ目だけ見ると誤診する）。
6. **メディアのURLを完全一致で突き合わせない（2026-08-29に踏んだ）。** 配信元をSupabase→R2へ移した際、cronの孤児掃除が「本文にこの完全URLが含まれるか」で削除対象を決めていたため、**236件中233件が削除対象**になった（空撃ちで発見、実行前に修正）。写真プールも `/scribe-media/` を含むかで絞っていて空になり、Homeのランダム写真が実際に消えた。**判定はパスで行う**（`lib/site/media.ts`）。
7. **CSPの `img-src` と `media-src` は別物（2026-07-30と2026-08-29に二度踏んだ）。** 画像は `imgThumb` が `/cdn-cgi/image/` 経由＝自ドメインに書き換えるのでCSPの許可が要らないが、**動画と音源は生のURLのまま出る**。配信元を増やしたら `media-src` に足さないと、**画像は無事なのに動画と音源だけが黙って消える**。
8. **R2の `put()` は長さの分からない `ReadableStream` を受け付けない（2026-08-29）。** `request.body` をそのまま渡すと保存の一歩手前で500になる。`arrayBuffer()` で渡す（上限50MB・Workerメモリ128MBなので収まる）。
9. **アップロードの許可はエディタとサーバーの両方にある。** 種類を増やすときは `lib/site/media.ts` の `ALLOWED_EXT` を直す（出所は1つ）。かつてエディタだけ音声を通してサーバーを忘れ、mp3が「アップロード失敗」の一言で弾かれ続けた。
10. **ブラウザのタブを使い回して検証しない。** 古いCSPや古いキャッシュを掴んだままのタブを見て「サイトが壊れた」と誤診し、実在しない障害を長時間追ったことがある（2026-08-29）。**確認は必ず新しいタブで。**
11. **分析API（GraphQL）の `exceededResources` はごく一部しか見せない（2026-08-29）。** 裏側（`waitUntil`）の失敗はリクエストとしては成功に数えられるため、実際の1/293しか出ない。**1102を調べるときは必ず Workers Observability のログを見る。**
12. **ブラウザのタブを使い回して検証しない**（再掲・実際に誤診した）。
13. **service binding越しの呼び出しは、呼び出し元とCPU・サブリクエストの予算を共有する（2026-08-31／09-02）。** ISRの作り直しをmemory-queueでやると、再描画（実測100〜900ms）が**閲覧者のリクエストの予算に乗る**。Durable Objectなら自分の予算で走る。「裏でやっているから読者には関係ない」は成り立たない。
14. **既定値が「何もしない実装」のことがある（2026-08-31）。** OpenNextのqueueは未指定だと `"dummy"`＝`send()` がFatalErrorを投げるだけ。**設定しなかった結果として、稼働開始から一度も作り直しが成功していなかった**。しかも読者には古いページが正常に返るので外からは壊れて見えない。**「設定しなかったらどうなるか」を既定値の実装まで見に行く。**
15. **`loading.tsx` はソフト404を作る（2026-09-10）。** フォールバックがストリーミングされた時点でHTTPステータス200が確定するため、その内側では `notFound()` がステータスを404にできない（noindexを差し込むのが精一杯）。**可変の子を持つフォルダに `loading.tsx` を置かない。**
16. **iTunes Search APIは名前が一致しなくても先頭を返す（2026-09-10）。** 「コーチングのコ」で検索すると、語がかすっただけの別番組が1件目に来る。そのまま採ると**他人の音源が別番組の名前で鳴る**ところだった。完全一致は無条件、部分一致は「短い方が4文字以上」「長さの比が0.5以上」のときだけ受け、**見つからなければnullを返す**（先頭に落とさない）。外部の検索APIは常にこの形で受ける。
17. **外から拾った日時はUTCのまま切らない（2026-09-10）。** Spotifyの `releaseDate` を `isoString` のまま日付に落とすと、RSS経由の日付と1日ずれる。棚の並びは公開日順なので、**出所によって同じ回の位置が動く**。東京日付に直してから使う。
18. **UAで返ってくるHTMLが変わる（2026-09-10）。** SpotifyにフルのChrome UAを送るとSPAの殻だけが返って `og:*` が1つも無く、素朴なUAだとメタ入りのHTMLが返る。取得に失敗したとき、**まずUAを疑う**（他社のbotを名乗らず、素性を書いたUAを送っている）。
19. **キャッシュの鍵を上げ忘れると古い形が焼き付く（2026-09-09。R2移行時にも同じ踏み方をした）。** 中身の**意味**が変わる変更（写真プールのdateを「アップロード日」から「掲載日」へ等）では、鍵の番号を上げる。上げないと失効までの間、古い解釈のデータが配られ続ける。
